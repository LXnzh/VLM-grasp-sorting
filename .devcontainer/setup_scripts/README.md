This folder contains setup script which are executed during the build of the docker container. Please add your setup scripts in the correspoing folder, depending if it requires root or user permissions. The scripts are executed in alphabetical order. 

The setup scripts need to have the file extension `.sh`.